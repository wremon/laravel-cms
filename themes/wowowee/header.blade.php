@section( 'header' ) 

{{-- Request::getClientIp() --}}
	<title>{{-- $pageTitle --}}</title>

	{{--<meta name="description" content="{{ Page::getMeta( 'meta_description' ) }}">
    <meta name="keywords" content="{{ Page::getMeta( 'meta_keywords' ) }}">--}}

	{{-- Asset::renderStyles( 'header' ); --}}
@endsection